import { defineStore } from "pinia";
import ApiService from "../services/api.service";
import * as constants from "../constants";

export const ekycStore = defineStore({
    id: "ekyc",
    state: () => ({}),
    actions: {
        async apiOcr(data) {

        }
    }
})